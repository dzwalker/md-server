// Markdown 内容与目录(TOC)的响应式布局常量（单位 px）

/** md 内容最大显示宽度 */
export const MD_MAX_WIDTH = 980;
/** md 与 TOC 之间的间距 */
export const TOC_GAP = 24;
/** 内容区左右 padding 合计（px-6 * 2） */
export const PADDING_X = 48;
/** 父容器低于此宽度时 TOC 改为悬浮 */
export const TOC_FLOAT_BREAKPOINT = 960;

/** 目录面板宽度可调范围（默认值 ~ 默认值+200） */
export const TOC_WIDTH_DEFAULT = 224;
export const TOC_WIDTH_MIN = 224;
export const TOC_WIDTH_MAX = 424;
